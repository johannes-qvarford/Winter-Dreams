#ifndef INCLUDED_CREDITSBUTTON
#define INCLUDED_CREDITSBUTTON

#include "Button.h"

////////////////////////////////////////////////////////////
// /A credits button is used to show the credits.
////////////////////////////////////////////////////////////
class CreditsButton : public Button {
public:
	////////////////////////////////////////////////////////////
	// /Create a credits button.
	////////////////////////////////////////////////////////////
	CreditsButton();

	~CreditsButton();
	////////////////////////////////////////////////////////////
	// /Do the following:
	// /fadeout, pop state, push credits state, fadein.
	////////////////////////////////////////////////////////////
	void activate();

private:

	bool mUpdated;
	sf::Sound mActivationSound;
	std::shared_ptr<sf::SoundBuffer> mSoundBuffer;
};

#endif