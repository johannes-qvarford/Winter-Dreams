#ifndef INCLUDED_EXITBUTTON
#define INCLUDED_EXITBUTTON

#include "Button.h"

////////////////////////////////////////////////////////////
// /A exit button is used to end the game
////////////////////////////////////////////////////////////
class ExitButton : public Button {
public:
	////////////////////////////////////////////////////////////
	// /Create a exit button.
	////////////////////////////////////////////////////////////
	ExitButton();

	~ExitButton();
	////////////////////////////////////////////////////////////
	// /Do the following:
	// /fade, pop
	////////////////////////////////////////////////////////////
	void activate();

private:

	bool mUpdated;
	sf::Sound mActivationSound;
	std::shared_ptr<sf::SoundBuffer> mSoundBuffer;
};

#endif